# len(x) is used to determine the length of a variable
# result = name.find(" ") (for first occurrence)
# result = name.rfind(" ") (for last occurrence)
# name = name.capitalize() (to capitalize the first letter of the string)
# name = name.upper() (to capitalize every single letter of the string)
# name = name.lower() (to lower case every single letter of the string)
# name.isdigit() (boolean value, either False or True: it says True if all the characters are digits. Otherwise, it's False)
# name.isalpha() (boolean value that says if all the characters are letters. If there is a space in the string, it'll return False)
# result = phone_number.count("-") (to count the amount of x in a string)
# phone_number = phone_number.replace("-", "") (this is to replace every selected character with a new one. ("x", "y") is how it's applied)

# print(help(str)) (to get a 'dictionary' of terms)
